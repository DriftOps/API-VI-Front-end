{
  "extends": "@vue/tsconfig/tsconfig.dom.json", // Ou algo similar
  "compilerOptions": {
    "composite": true,
    "paths": {
      "@/*": ["./src/*"] // Ajuste se seu @ for diferente
    },
  },
  "include": [
    "env.d.ts",       // Ou o nome do seu shim file
    "src/**/*.ts",
    "src/**/*.tsx",
    "src/**/*.vue"

  ],
  "exclude": ["src/**/__tests__/*"], // Se você tiver testes
}